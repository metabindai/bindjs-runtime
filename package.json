{
  "name": "@metabindai/bindjs",
  "version": "0.0.0",
  "private": true,
  "description": "BindJS runtime + React renderer packages",
  "type": "module",
  "packageManager": "pnpm@10.13.0",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "clean": "pnpm -r exec rm -rf dist"
  },
  "devDependencies": {
    "typescript": "~5.6.2"
  }
}
